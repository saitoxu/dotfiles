#!/bin/bash

echo "use adauction3;" > join.sql
for i in `seq 1 100`
do
    echo "create table join${i} select bid${i}.bid_id, bid, winning_bid from bid${i} left join winning_bid${i} on bid${i}.bid_id = winning_bid${i}.bid_id;"
done >> join.sql
