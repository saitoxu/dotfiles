#!/bin/bash

i=1
echo "use adauction3;" > add_distance.sql
while IFS="|" read media_info path_id b w distance
do
    str="update bid${i} set bid = bid + (${distance});"
    echo $str
    i=`expr $i + 1`
done < avg_distance.back.csv >> add_distance.sql
